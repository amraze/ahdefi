export default function AddGoalModalStyles(theme) {
    const style = {
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: 'translate(-50%, -50%)',
        width: 400,
        bgcolor: theme.background,
        color: theme.text,
        borderRadius: 5,
        borderColor: theme.borders,
        boxShadow: 24,
        outline: 'none',
        p: 4,
    };

    return (style)
}
